# Disable Spring Cloud Config for tests to prevent connection errors
spring.cloud.config.enabled=false

# Provide a dummy issuer-uri for security configuration to load successfully
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://dummy-auth-server.com
